.banner
  .banner-text
    %h2
      %strong What do you crave on a Saturday Evening?
    %h2 Welcome to a place where good movies come to have Fun!
  .banner-actions
    %h3
      -if user_signed_in?
        = "Welcome, #{current_user.name}"
      -else
        = link_to "Sign In", new_user_session_path, class: "button form-btn"
        = link_to "Sign Up", new_user_registration_path, class: "button form-sub"

- @movies.each do |movie|
  %p= link_to movie.title, movie
%p= link_to "--> New Movie", new_movie_path
%p= will_paginate @collection, renderer: BootstrapPagination::Rails, previous_label: "Previous", next_label: "Next", inner_window: 1, outer_window: 0
